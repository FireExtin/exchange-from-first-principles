# 93 Command Log Replay Prototype

[English](#english) · [中文](#中文)

## English

Purpose: preserve the original runnable command-log replay prototype.

This appendix prototype shows how ordered commands and deterministic replay can
reach the same funds balances as a serial transaction model. It is a prototype
bridge toward the version line, not the new v1/v2 implementation.

## Status And Run

Status: runnable Go prototype.

Run:

```bash
go test ./...
```

## 中文

目的：保留原来的可运行 command-log replay 原型。

这个 appendix prototype 展示有序命令和确定性重放如何达到与串行事务模型相同的
资金余额。它是通往版本线的原型桥梁，不是新的 v1/v2 实现。

## 状态与运行

状态：可运行 Go 原型。

运行：

```bash
go test ./...
```
